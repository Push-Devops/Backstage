apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Backstage-app.fullname" . }}-test-connection"
  labels:
    {{- include "Backstage-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Backstage-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
